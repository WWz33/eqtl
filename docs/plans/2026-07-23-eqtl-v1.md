# eqtl v1 Implementation Plan

> **For Hermes:** Execute task-by-task; after full v1, run subagent review (not per-task).

**Goal:** Ship C++ CLI `eqtl` for cis/trans/gw eQTL with models `lm|glm|lmm|glmm`, htslib VCF (GT only), GFF TSS, GCTA-like pheno/cov/GRM, full association (not quasar score), tensorqtl-class empirical p.

**Architecture:** Single binary flag CLI. One VCF session; ID-overlap sample_order from pheno; stream SNP blocks for IO; prep (eig K / QR X) then per-SNP full model. `--fast` amortizes per-gene nuisance (δ/φ/VC), SNP still in mean.

**Tech Stack:** C++17, OpenMP, vendored htslib 1.24, Eigen 3.4 headers, optional GSL; no system/popen to gemma/gcta.

**Repo root:** `/home/ww/eqtl/eqtl`

---

## LOCK summary (do not reopen)

- Binary: `eqtl`; no subcommands; `--make-grm` path
- Models: lm, glm=NB, lmm (default), glmm=Poisson; multi via comma
- Engine: full association; NOT residualise+score
- `--fast`: lmm per-gene δ; glm per-gene φ; glmm per-gene VC; lm no-op
- Dosage: GT→0/1/2 only; ignore DS
- `--miss-hand` default **filter**
- Mode: cis|trans|all default all; no GFF → gw all-pairs
- Cis: TSS±1Mb; + start / - end
- Emp p: tensorqtl-class `--perm` default 10000; p_emp + p_beta; phenotype permute
- ACAT gene-level; top+pairs; p-threshold 1e-5; plain TSV no gz
- Threads: `--thread N` IO-max (stream SNP blocks)
- Non-goals: APL, score engine, residualise mode, PLINK, GPU, LOCO, g×E

---

### Task 1: Scaffold repo

**Objective:** Build skeleton that compiles empty main + help.

**Files:**
- Create: `Makefile`, `.gitignore`, `.gitmodules`, `README.md`, `README.zh.md`, `LICENSE`
- Create: `include/eqtl/*.hpp` stubs, `src/main.cpp`
- Create: `third_party/` (htslib submodule later; Eigen headers)

**Steps:**
1. Copy Makefile pattern from `/home/ww/xpclr/xpclr-cpp/Makefile` (htslib+OpenMP); add Eigen `-Ithird_party/eigen`
2. Vendor Eigen: copy or link `quasar/external_libs/eigen-3.4.0` → `third_party/eigen`
3. Init htslib submodule tag 1.24 OR USE_SYSTEM_HTS if build blocked
4. `src/main.cpp`: print help, `--version`
5. `make -j` → binary `eqtl`; `./eqtl -h` exit 0

---

### Task 2: CLI parse

**Objective:** Parse all LOCK flags into `Options` struct.

**Files:**
- Create: `include/eqtl/options.hpp`, `src/options.cpp`

**Flags:**
```
--vcf --pheno --out [--gff] [--covar] [--grm]
--make-grm
--mode cis|trans|all
--model lm,glm,lmm,glmm
--window --pval-cis --pval-trans
--miss-hand filter|impute
--fast --thread --perm --seed
--disable-beta-approx
--gff-id-key
-h/--help --version
```

**Verify:** `./eqtl --help`; missing required → non-zero + short message

---

### Task 3: Pheno / covar / sample align

**Objective:** Load GCTA-like matrices; compute sample_order.

**Files:**
- Create: `include/eqtl/pheno.hpp`, `src/pheno.cpp`

**Rules:**
- Pheno: col0 = sample ID, other cols = genes (header row with gene names)
- Covar optional: col0 = ID, rest covariates; auto intercept if GCTA-like missing
- Overlap IDs; N=0 error; order = pheno file order among overlap

**Verify:** unit-style smoke on tiny TSV in `data/tiny_*`

---

### Task 4: GFF → TSS

**Objective:** Map gene_id → (chr, tss, strand) from GFF3.

**Files:**
- Create: `include/eqtl/annot.hpp`, `src/annot.cpp`

**Rules:**
- Feature type gene (or configurable); attr key `--gff-id-key` default ID/Name/gene_id try
- + → start, - → end as TSS
- 0 hit skip; multi warn+skip
- No GFF → all genes gw mode

---

### Task 5: VCF session + GT dosage

**Objective:** One htslib session; stream SNPs; GT→0/1/2; miss filter/impute.

**Files:**
- Create: `include/eqtl/vcf_session.hpp`, `src/vcf_session.cpp`

**Rules:**
- Open once; index warn once if missing
- Sample subset reorder to sample_order
- Multi-allelic: skip or first ALT only (document: skip multi-allelic)
- miss filter: exclude sample pairs / drop SNP if too many missing (define: drop SNP if any missing in analysis samples when filter; impute: mean dosage)

**Verify:** read smoke VCF subset, print n_snps n_samples

---

### Task 6: GRM read/write/compute

**Objective:** GCTA .grm.id + .grm.bin; --make-grm; auto compute for lmm/glmm.

**Files:**
- Create: `include/eqtl/grm.hpp`, `src/grm.cpp`

**Rules:**
- float32 lower triangle incl diagonal, length n(n+1)/2
- ID match extract/reorder
- Compute: standardized GT crossprod / p (classic GRM sketch)

---

### Task 7: Model lm

**Objective:** Exact OLS with covariates; SNP in model.

**Files:**
- Create: `include/eqtl/model_lm.hpp`, `src/model_lm.cpp`

**Math:** Project y,g orthogonal to X (QR); β, se, t, p, r2

**Gold:** tiny design vs numpy/MatrixEQTL ≤1e-4

---

### Task 8: Model lmm

**Objective:** Spectral LMM full association; --fast per-gene δ.

**Files:**
- Create: `include/eqtl/model_lmm.hpp`, `src/model_lmm.cpp`

**Math:**
1. eigendecompose K = QΛQᵀ once
2. rotate y,X
3. per SNP (or shared δ if --fast): estimate δ, then β_g, se, p in diagonal metric

**Gold:** small set vs GEMMA -lmm when available; --fast also has reference numbers in data/

---

### Task 9: Model glm (NB)

**Objective:** NB GLM with g in design; --fast freezes φ per gene.

**Files:**
- Create: `include/eqtl/model_glm.hpp`, `src/model_glm.cpp`

**Math:** IRLS NB; offset log(lib size) optional if counts; no APL
**Gate:** reject non-count pheno for glm/glmm

---

### Task 10: Model glmm (Poisson)

**Objective:** Poisson GLMM with g in fixed effects; full cis+trans; --fast freeze VC per gene.

**Files:**
- Create: `include/eqtl/model_glmm.hpp`, `src/model_glmm.cpp`

**Math:** PQL-style; g in mean; per-SNP update β_g (full) or freeze RE/--fast

---

### Task 11: Scan driver + output

**Objective:** cis/trans/all/gw loops; write pairs/top/region.

**Files:**
- Create: `include/eqtl/scan.hpp`, `src/scan.cpp`, `src/output.cpp`

**Outputs:**
```
{out}.{model}.cis.pairs.tsv
{out}.{model}.cis.top.tsv
{out}.{model}.cis.region.tsv   # ACAT + emp columns
... trans / gw
```

Columns per LOCK. p-threshold filter on pairs/top.

---

### Task 12: ACAT + empirical p

**Objective:** tensorqtl-class perm + beta approx + ACAT.

**Files:**
- Create: `include/eqtl/stats_extra.hpp`, `src/stats_extra.cpp`

**Rules:**
- --perm default 10000; 0 off
- p_emp + p_beta default; --disable-beta-approx
- permute phenotype; T = min p or max r2 for lm path

---

### Task 13: Smoke data + integration

**Objective:** 100 samples × 1000 genes from real VCF+GFF; runnable smoke.

**Files:**
- Create: `scripts/make_smoke.sh`, `data/` inputs + gold snippets
- Sources: `/home/ww/eqtl/373_xas_ld5.vcf.gz`, `wm82_T2T.gff_agat`

**Verify:**
```
make -j
./eqtl --vcf data/smoke.vcf.gz --pheno data/smoke.pheno.tsv --gff data/smoke.gff \
  --model lm --mode cis --perm 0 --out data/smoke_out
# check pairs non-empty for synthetic signal gene
```

---

### Task 14: Slim README + help polish

**Objective:** Getting Started only; bcftools-shaped help; zh mirror.

---

### Task 15: Subagent review of v1

**Objective:** After green smoke, delegate review (architecture + risks + CLI/LOCK compliance). Fix criticals.

---

## Implementation order note

Tasks 1–6 foundation; 7–10 models can share `AssocResult {beta,se,stat,p,r2,maf,...}`; 11–12 wire; 13 data; 15 review.

## Commit style

Short human subjects: `scaffold: makefile + help`, `io: vcf session GT`, `model: lm ols`, ...
